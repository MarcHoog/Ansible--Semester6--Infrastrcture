#! Makes use of the terraform role
---
- hosts: testnode
  name: Deploy the terraform role on the terraform group
  tasks:
    - name: 'Deploying, operator applied'
      include_role:
        name: terraform
