---
- hosts: ansible
  tasks:
      - name: Creating the user ansible-op
        become: true
        user:
          name:  "ansible-op"
          create_home: true
          home: /home/ansible-op
          move_home: true
          shell: "/bin/bash"
