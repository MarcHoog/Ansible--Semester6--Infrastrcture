---

- name: pinging
  hosts: all
  tags: everyone
  roles:
    - ansible
    - qemuguest

  tasks:

    - name: install neofetch
      ansibe.builtin.apt:
        name:  neofetch
        state: present
      become: true
      register: result
      until: result is successful
          