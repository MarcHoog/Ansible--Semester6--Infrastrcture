---

- name: pinging
  hosts: all
  tags: everyone
  roles:
    - ansible
    - qemuguest

  tasks:

    - name: install neofetch
      become: true
      ansibe.builtin.apt:
        name:  neofetch
        state: present
      register: result
      until: result is successful
          