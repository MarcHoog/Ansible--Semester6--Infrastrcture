---
- name: install kubernetes
  hosts: kubernetes_workers
  tasks:

    - name: include kubernetes role with worker var
      include_role:
        name: kubernetes
      vars:
        role: worker

